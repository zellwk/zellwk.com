---
layout: _layout/page
pagination:
  data: collections
  size: 1
  alias: tag
  filter:
    - post
    - all
permalink: /tags/{{ tag | slug}}/
noIndex: true
---
<h1>Tagged with “{{ tag }}”</h1>

{% markdown %}
Here are all articles tagged with {{tag}}. Enjoy!
{%- endmarkdown %}
<ol>
  {%- set taglist = collections[ tag ] -%}

  {%- for post in taglist | reverse %}
    {%- isPublished post.date %}
    <li>
      <a href="{{ post.url | url }}">{{ post.data.title | markdown }}</a>
    </li>
    {%- endisPublished %}
  {%- endfor %}
</ol>
