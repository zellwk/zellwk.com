---
layout: _layout/html
permalink: /blog/{{ slug }}/index.html
newsletter: better-fed
footer: true
---

<div class="wrap">
  <article class="post">
    <header>
      <div class="kite"></div>
      <div>
        <h1>{{title | markdown | safe}}</h1>
        <div class="meta">
          <p>Written on: <date datetime="{{page.date}}">{{page.date | readableDate | lower }}</date>
          </p>

          <div class="tags">
            <span>Filed under: </span>
            <ul>
              {% for tag in tags %}
                <li>
                  <a href="/tags/{{tag}}">#{{tag }}</a>
                </li>
              {% endfor %}
            </ul>
          </div>
        </div>
      </div>
    </header>

    <div class="longform">
      {% import "_macros/media.njk" as media %}
      {{media.youtube(youtubeHash)}}

      {{- content | safe -}}

      {# Thanks part #}
      {%- markdown -%}
      {% set link = site.url + page.url %}

      If you enjoyed this article, please tell a friend about it! Share it on {% twitterLink title,
      link %}. If you spot a typo, I'd appreciate if you can correct [it on GitHub](https://github.com/zellwk/zellwk.com/blob/master/{{ page.inputPath }}). Thank you!

      {% if series %}
        <section class="o-words series-layout">
          <h2>Other articles in this series</h2>
          <ol>
            {% for article in collections.seriesContent %}
              {% if article.data.series == series %}
                <li>
                  {% if article.url == page.url %}
                    <span>{{article.data.title}}</span>
                  {% else %}
                    <a href="{{article.url}}">{{article.data.title | markdown}}</a>
                  {% endif %}
                </li>
              {% endif %}
            {% endfor %}
          </ol>
        </section>
      {% endif %}

      <section id="convertkit" class="longform jsCkForm">
        {%- include 'convertkit.njk' -%}
      </section>

      {%- endmarkdown %}
    </div>
  </article>

  <nav class="pagination">
    {% for post in collections.publishedPosts -%}
      {% if post.inputPath == page.inputPath -%}
        {%- set nextPost = collections.publishedPosts[loop.index0 - 1] %}
        {%- set previousPost = collections.publishedPosts[loop.index0 + 1] %}

        {% if previousPost.url -%}
          <div>
            <span>Prev article:</span>
            <a href="{{previousPost.url}}">
              {{previousPost.data.title}}
            </a>
          </div>
        {% endif -%}

        {% if nextPost.url -%}
          <div>
            <span>Next article:</span>
            <a href="{{nextPost.url}}">
              {{nextPost.data.title}}
            </a>
          </div>
        {% endif -%}
      {% endif -%}
    {% endfor -%}
  </nav>
</div>
