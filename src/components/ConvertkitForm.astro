---
import ConvertkitForm from './ConvertkitForm.svelte'
---

<!-- This actually already uses Sendy, but the name is still ConvertkitForm because... haven't change yet.  -->
<ConvertkitForm client:idle />
